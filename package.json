{
	"name": "tru-met",
	"version": "1.0.0",
	"description": "\"A company specializing in cutting and bending metal sheet components",
	"main": "index.js",
	"scripts": {
		"dev": "concurrently \"npx tailwindcss -i ./src/main.css -o ./dist/main.css --watch\" \"live-server --port=8080\"",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"author": "",
	"license": "ISC",
	"devDependencies": {
		"autoprefixer": "^10.4.16",
		"concurrently": "^8.2.2",
		"live-server": "^1.1.0",
		"postcss": "^8.4.32",
		"tailwindcss": "^3.4.0"
	},
	"dependencies": {
		"boxicons": "^2.1.4"
	}
}
